import random
import string

val = string.ascii_letters + string.digits + string.punctuation

pss_len = 12

password = "".join([random.choice(val) for i in range(pss_len)])

print(f"This is your strong password: {password}")
